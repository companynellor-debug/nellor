// NegotiationContractPDF was removed as part of inactive features cleanup
// Stub export to prevent import errors
export const generateNegotiationPDF = (_data: any) => {
  console.warn('generateNegotiationPDF: feature temporarily unavailable');
};
